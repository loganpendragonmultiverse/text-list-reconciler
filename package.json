{
  "name": "text-list-reconciler",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A private, browser-based comparison tool for arbitrary text lists.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/core.js && node --check src/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
